<!DOCTYPE html>
<html lang="kr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('nav.ejs') %>

  <!-- 서버로 이미지를 올리기위해 enctype 설정 -->
  <form action="/add" method="POST" enctype="multipart/form-data">
    <h3>글작성</h3>
    <input name="title" type="text" placeholder="제목"><br>
    <input name="content" type="text" placeholder="내용"><br>
    <input name="img1" type="file"  accept="image/*">
    <!-- img파일을 가져가는걸 권장하기위한 accept -->
    <!-- 이미지를 여러장 넣고싶다면 인풋에 multiple 속성 추가
      + server.js 에 upload.array('인풋name명', 사진최대장수(숫자))
    -->

    <button type="submit">작성</button>
  </form>
</body>
</html>
